# 智能花盆项目开发规范

## 项目目标

本项目用于完成基于 ESP32-S3 的智能花盆毕业设计。交付物包括可稳定演示的实物系统、完整源代码、接线与测试记录，以及论文和答辩所需技术资料。

## 开发原则

1. 按功能逐项开发；当前功能完成编译和实机验证后，才能进入下一项。
2. 每次只修改一个功能点，避免同时引入多个无法独立定位的问题。
3. 优先保证答辩演示的稳定性，不增加不影响验收结果的扩展功能。
4. 禁止通过注释掉故障代码、跳过校验或隐藏错误来获得表面成功。

## 目录约定

- 每个硬件验证功能使用独立的 Arduino 草图目录。
- `test_minimal/` 保存板卡和串口最小验证程序。
- `step2_sensor_test/` 作为已有的多传感器集成代码参考，不直接承载后续正式固件。
- `docs/` 保存设计、计划、接线说明和测试记录。

## 代码规范

1. Arduino/C++ 代码遵循 Arduino 官方常用风格，使用两个空格缩进。
2. 常量采用 `UPPER_SNAKE_CASE`，函数和变量采用 `lowerCamelCase`。
3. 所有函数必须添加中文功能注释；参数和返回值存在时必须说明。
4. 只保留必要的技术注释，禁止对话式、需求复述式和冗余注释。
5. GPIO、波特率、时间间隔和阈值必须使用具名常量。
6. 禁止在代码和日志中写入 Wi-Fi 密码、Token 等敏感信息。

## 硬件安全

1. 水泵不得通过 ESP32 的 3.3V 引脚供电。
2. 继电器和水泵接入前必须先完成空载逻辑测试。
3. 自动和手动浇水都必须受最长运行时间保护。
4. 土壤湿度传感器异常时禁止自动启动水泵。
5. 修改继电器有效电平、供电方式或引脚定义前必须先确认实物规格。

## 验证要求

1. 使用 Arduino CLI 对草图进行编译验证。
2. 默认通用板卡目标为 `esp32:esp32:esp32s3`；实物型号确定后更新为准确 FQBN。
3. 涉及硬件的功能必须完成烧录和实机观察，并记录串口输出或现象。
4. 未连接实物时只能声明“编译通过”，不能声明“功能验证通过”。
5. 每次交付必须说明修改文件、编译结果、待执行的实机步骤和预期结果。

## 变更边界

删除文件、修改密钥或环境配置、安装全局依赖、数据库变更、部署和公开发布前必须获得明确确认。
